<nav class="navbar navbar-expand-md navbar-dark">
	<a class="navbar-brand" href="{{ path('tricks_index') }}">
    	<img src="{{ asset('img/logo.png') }}" alt="" class="navbar-logo">
  	</a>
    <div class="collapse navbar-collapse" id="navbarNav">
    	<ul class="navbar-nav">
    		<li class="nav-item">
    			<a class="nav-link" href="{{ path('tricks_index') }}">Home</a>
    		</li>
    		<li class="nav-item">
    			<a class="nav-link" href="{{ path('tricks_new') }}">Tricks</a>
    		</li>
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Sign Out</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Sign in</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_registration') }}">Sign up</a>
                </li>
            {% endif %}
            
    	</ul>
    </div>
</nav>